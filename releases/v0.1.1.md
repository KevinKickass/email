Initial release candidate for **email**. The Linux release workflow did not complete; use v0.1.2 or a later published release.

This candidate introduced the classic German/English interface, IMAP/SMTP, redb storage, optional signed updates, the Linux NVIDIA rendering workaround and the Apache-2.0 license.
