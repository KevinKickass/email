[package]
name = "mail-store"
license = "Apache-2.0"
version = "0.1.0"
edition = "2021"
rust-version = "1.90"

[dependencies]
redb = "4.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
